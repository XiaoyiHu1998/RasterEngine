#version 330
layout(location = 0)in vec4 vPosition;

out vec4 vertexPos;
uniform vec4 location;
uniform mat4 mvpMatrix;

void main()
{
    vertexPos = vPosition + (location); //for color


    gl_Position = mvpMatrix * vPosition;
}